# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=contact_hub
DB_USER=postgres
DB_PASSWORD=postgres
# Use 'db' when running via docker compose, 'localhost' otherwise
DB_HOST=db
DB_PORT=5432

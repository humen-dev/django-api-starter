[project]
name = "contact-hub-api"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.7",
    "gunicorn>=26.0.0",
    "psycopg2-binary>=2.9.12",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
]
